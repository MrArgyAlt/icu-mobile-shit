function opponentNoteHit(id, direction, noteType, isSustainNote)
  if dadName == 'peter-mad' then
   setProperty("health", getProperty("health") - 0.02)
		if getProperty("health") < 0.1 then
			setProperty("health", 0.1)
      end
    end
end